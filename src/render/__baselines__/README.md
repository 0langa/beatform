# GPU pixel baseline

`gpu-pixel-matrix.json` is generated from real Tauri WebView2/WebGPU by:

```powershell
npm run test:gpu:update
```

Ordinary validation uses `npm run test:gpu`. Update only after intentional
visual changes have been reviewed.
